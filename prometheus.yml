global:
  scrape_interval: 15s

scrape_configs:
# Prometheus
- job_name: "prometheus"
  static_configs:
  - targets: [ "prometheus:9090" ]

# Node Exporter metrics
- job_name: "node_exporter"
  static_configs:
  - targets: [ "node-exporter:9100" ]
    labels:
      nodename: "ec2-server"
